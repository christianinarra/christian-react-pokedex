import React from 'react';
import loading from '../loading.gif';

export default () => (
  <div style={{textAlign: 'center', marginTop:'5%'}}>
    <img src={loading} style={{maxWidth: '350px'}} />
  </div>  
);
